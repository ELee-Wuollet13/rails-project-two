<h1>Add a new review:</h1>

<%= form_for [@project, @review] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <p><%= f.submit %></p>
<% end %>
